<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeroone.star.othermana.mapper.ContTypeMapper">
    <!--<update id="updateBaContType" parameterType="com.zeroone.star.project.dto.j8.other.baconttype.BaContTypeDTO">
        update zo_wms.ba_cont_type
        <set>
            <if test="contTypeName != null">cont_type_name=#{contTypeName},</if>
            <if test="contTypeCode != null">cont_type_code=#{contTypeCode}</if>
        </set>
        where id=#{id}
    </update>-->
</mapper>

